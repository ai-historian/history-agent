Display a scanned page image to the user in the viewer. Pass `source` (the collection member ref) and `page_id`. This does NOT analyze the page — use the task tool for that. Use this when you want to show the user a specific page.
